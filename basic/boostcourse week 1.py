'''
Q4. 우리는 방금 CSV 파일을 읽는 함수를 구현해보았습니다.
하지만 이를 조금 더 효율적으로 사용하기 위해서 클래스로 구성을 진행하려고 합니다.
방금 구현한 함수를 포함한 클래스를 구현해보세요.

- merge list를 이용해 리스트 내 데이터의 합을 출력해보세요.
- 데이터를 합치기 전 데이터의 자료형을 변경해보세요.
'''
# 파일의 경로를 file_path 로 설정
# ex) file_path = "./ddata-01-test-score.csv"

class ReadCSV():
    def __init__(self, file_path):


    def read_file(self, #):
        pass

    def merge_list(self, #):
        pass




'''
Q5. 이전에 구현한 클래스에서 merge_list의 함수 동작을 변경해야합니다. 단순합계가 아닌 평균을 구하는 함수로 변경해보세요.
- 리스트의 데이터를 다루는 함수를 이용해서 구현해보세요.
- 최종 평균을 구한 후 오름차순으로 정렬해주세요.

'''
# 파일의 경로를 file_path 로 설정
# ex) file_path = "./data-01-test-score.csv"

class ReadCSV():
    def __init__(self, file_path):

    def read_file(self, #):

    def merge)list(self, #):




